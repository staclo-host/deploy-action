/**
 * @description
 * The wrapper that starts/stops a docker compose file around a callback.
 * Performs docker compose up, then runs `callback` function and after its promise fulfilled,
 * runs docker compose down.
 */
export declare function withDockerCompose<T>(dockerComposeYmlPath: string, callback: () => Promise<T>, printDebug?: boolean, composeOptions?: string[]): Promise<T>;
//# sourceMappingURL=withDockerCompose.d.ts.map