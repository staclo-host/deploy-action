"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TimeoutDetector = void 0;
const Duration_1 = require("../../../utils/Duration");
class TimeoutDetector {
    static start(timeoutMs) {
        return new TimeoutDetector(Duration_1.Duration.startMeasuring(), timeoutMs);
    }
    constructor(duration, timeoutMs) {
        this.duration = duration;
        this.timeoutMs = timeoutMs;
    }
    measure() {
        const durationMs = this.duration.measureMs();
        return {
            durationMs,
            timedOut: this.timeoutMs
                ? this.duration.measureMs() > this.timeoutMs
                : false
        };
    }
}
exports.TimeoutDetector = TimeoutDetector;
//# sourceMappingURL=TimeoutDetector.js.map