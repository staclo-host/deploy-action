import { Duration } from "../../../utils/Duration";
export declare class TimeoutDetector {
    readonly duration: Duration;
    readonly timeoutMs: number | undefined;
    static start(timeoutMs: number | undefined): TimeoutDetector;
    constructor(duration: Duration, timeoutMs: number | undefined);
    measure(): {
        durationMs: number;
        timedOut: boolean;
    };
}
//# sourceMappingURL=TimeoutDetector.d.ts.map